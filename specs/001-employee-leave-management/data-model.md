# Data Model: Employee Leave Management MVP

## Modeling conventions

- PostgreSQL UUID primary keys are generated by the application or database and exposed through REST DTOs.
- Business timestamps use `timestamptz` in UTC. Leave dates use `date` and are interpreted in the configured organization time zone.
- Leave quantities are stored as integer `units`, where `1` unit is one half-day. REST DTOs expose `days` as multiples of `0.5`.
- Mutable aggregate roots carry `version bigint` for optimistic stale-write detection; balance-changing commands additionally use pessimistic row locks.
- Audit and status/movement history tables are append-only. Normal master-data removal is represented by deactivation.
- Human-entered reasons and comments have explicit nonblank and length constraints. Password hashes, session identifiers, and sensitive security data never appear in audit payloads.

## Relationships

```text
UserAccount --< UserAccountRole >-- Role
     |
     | 1:0..1
     v
EmployeeProfile -- manager_id --> EmployeeProfile
     |                         
     |--< LeaveBalance >-- LeaveType --< LeavePolicyVersion
     |          |                  |              |--< PolicyWeeklyOff
     |          |                  |              `--< PolicyHolidayRule
     |          |                  `--< CompanyHoliday (used by calculation)
     |          |
     |          `--< LeaveBalanceMovement
     |
     `--< LeaveRequest --< LeaveRequestSlot
              |  |--< LeaveRequestBalanceLine >-- LeaveBalance
              |  `--< LeaveRequestStatusHistory
              `--< AuditEvent (by reference)
```

## Entities

### OrganizationSettings

Single-organization settings needed to interpret configurable dates.

| Field | Type | Rules |
|---|---|---|
| `id` | UUID | Primary key; exactly one active row for MVP |
| `name` | varchar(160) | Nonblank |
| `time_zone_id` | varchar(64) | Valid IANA time-zone identifier |
| `active` | boolean | Must be true for the current organization |
| `version` | bigint | Increments on change |

### UserAccount

Authentication identity independent of employee profile lifecycle.

| Field | Type | Rules |
|---|---|---|
| `id` | UUID | Primary key |
| `login` | varchar(254) | Display form; nonblank |
| `normalized_login` | varchar(254) | Lowercased/normalized; unique |
| `password_hash` | varchar(255) | Required; never returned or audited |
| `enabled` | boolean | Disabled accounts cannot authenticate |
| `credentials_updated_at` | timestamptz | Required |
| `created_at`, `updated_at` | timestamptz | Required |
| `version` | bigint | Stale update guard |

### Role and UserAccountRole

`Role` contains the fixed authorization codes `EMPLOYEE`, `MANAGER`, and `ADMINISTRATOR`. `UserAccountRole` is a unique pair of account and role, allowing legitimate multi-role users.

### EmployeeProfile

| Field | Type | Rules |
|---|---|---|
| `id` | UUID | Primary key |
| `employee_number` | varchar(64) | Unique, nonblank business identifier |
| `user_account_id` | UUID | Unique FK to `user_account` |
| `display_name` | varchar(160) | Nonblank |
| `email` | varchar(254) | Nonblank; normalized uniqueness if used as login/contact |
| `manager_id` | UUID nullable | Self-FK to `employee_profile`; cannot equal `id` |
| `active` | boolean | Inactive employees cannot submit new requests |
| `created_at`, `updated_at` | timestamptz | Required |
| `version` | bigint | Stale update guard |

Manager assignment validation also requires the target profile to be active and its account to hold the `MANAGER` role. This cross-table rule is enforced by the people application service and integration tests. The manager relationship is the only manager approval scope in the MVP.

### LeaveType

Stable leave-category identity. Policy values are versioned separately so existing request context remains explainable.

| Field | Type | Rules |
|---|---|---|
| `id` | UUID | Primary key |
| `code` | varchar(40) | Unique normalized code |
| `name` | varchar(120) | Nonblank |
| `description` | varchar(500) nullable | Optional user-facing description |
| `active` | boolean | Inactive types cannot be used for new requests |
| `created_at`, `updated_at` | timestamptz | Required |
| `version` | bigint | Stale update guard |

### LeavePolicyVersion

Effective-dated configuration for one leave type.

| Field | Type | Rules |
|---|---|---|
| `id` | UUID | Primary key |
| `leave_type_id` | UUID | FK to `leave_type` |
| `version_number` | integer | Positive; unique per leave type |
| `effective_from` | date | Required |
| `effective_to` | date nullable | Not before `effective_from`; null means open-ended |
| `tracks_balance` | boolean | Whether approval affects a leave balance |
| `balance_validation_required` | boolean | Implies `tracks_balance`; submission reserves units |
| `allows_half_day` | boolean | Controls half-day request option |
| `weekly_off_treatment` | enum | `EXCLUDE` or `INCLUDE` |
| `holiday_treatment` | enum | `EXCLUDE` or `INCLUDE` |
| `rejection_comment_required` | boolean | Manager validation rule |
| `cancellation_cutoff_days` | integer | Nonnegative calendar days before start date |
| `active` | boolean | At most one effective version on a date |
| `created_by_user_id` | UUID | Administrator actor |
| `created_at` | timestamptz | Required |

Policy date ranges for the same leave type must not overlap. For approved self-cancellation, calculate the cutoff as the start of `start_date - cancellation_cutoff_days` in the organization time zone; self-cancellation is allowed only before that instant. Pending requests may be cancelled before a decision.

### PolicyWeeklyOff

| Field | Type | Rules |
|---|---|---|
| `policy_version_id` | UUID | FK to policy version |
| `day_of_week` | smallint | ISO value 1 through 7 |

Primary key is `(policy_version_id, day_of_week)`. This permits configurable weekly-off patterns rather than fixed weekends.

### CompanyHoliday

| Field | Type | Rules |
|---|---|---|
| `id` | UUID | Primary key |
| `holiday_date` | date | Unique within the organization |
| `name` | varchar(160) | Nonblank |
| `active` | boolean | Inactive holidays are ignored by new calculations |
| `created_by_user_id` | UUID | Administrator actor |
| `created_at`, `updated_at` | timestamptz | Required |
| `version` | bigint | Stale update guard |

### LeaveBalance

Current summary for an employee, leave type, and nonoverlapping policy period.

| Field | Type | Rules |
|---|---|---|
| `id` | UUID | Primary key |
| `employee_id` | UUID | FK to employee |
| `leave_type_id` | UUID | FK to leave type |
| `period_start`, `period_end` | date | Required; end is not before start |
| `allocated_units` | integer | Nonnegative |
| `adjustment_units` | integer | Signed administrator adjustments |
| `reserved_units` | integer | Nonnegative |
| `consumed_units` | integer | Nonnegative |
| `version` | bigint | Lock/version guard |
| `created_at`, `updated_at` | timestamptz | Required |

Unique key: `(employee_id, leave_type_id, period_start, period_end)`. Periods for the same employee and leave type must not overlap.

Derived quantities:

- `entitled_units = allocated_units + adjustment_units`
- `available_units = entitled_units - reserved_units - consumed_units`

For a balance-validated policy, `available_units` cannot fall below zero. A tracked but non-validating policy may permit negative availability according to configuration. An untracked policy has no balance or balance line.

### LeaveRequest

Aggregate root for an employee's requested leave.

| Field | Type | Rules |
|---|---|---|
| `id` | UUID | Primary key |
| `employee_id` | UUID | FK to employee; immutable |
| `leave_type_id` | UUID | FK to leave type; immutable after submit |
| `submitted_policy_version_id` | UUID | Policy used for submitted calculation |
| `start_date`, `end_date` | date | Required; start is not after end |
| `duration_mode` | enum | `FULL_DAY`, `HALF_DAY_AM`, `HALF_DAY_PM` |
| `chargeable_units` | integer | Positive; equals materialized request slots |
| `reason` | varchar(1000) | Nonblank |
| `status` | enum | `PENDING`, `APPROVED`, `REJECTED`, `CANCELLED` |
| `submitted_at` | timestamptz | Required |
| `decided_at`, `decided_by_user_id` | nullable | Both populated for approval/rejection |
| `decision_comment` | varchar(1000) nullable | Required for rejection when policy requires |
| `cancelled_at`, `cancelled_by_user_id` | nullable | Both populated for cancellation |
| `policy_snapshot` | jsonb | Calculation-relevant submitted rule values, no secrets |
| `idempotency_key` | varchar(100) nullable | Unique per submitting user/command |
| `version` | bigint | Stale decision guard |

`HALF_DAY_AM` and `HALF_DAY_PM` require `start_date = end_date` and an effective policy that allows half days. Status and audit history are never inferred solely from nullable timestamps.

### LeaveRequestSlot

Materialized occupied half-day slots used for calculation evidence and overlap protection.

| Field | Type | Rules |
|---|---|---|
| `id` | UUID | Primary key |
| `request_id`, `employee_id` | UUID | Composite FK to the request and its owner |
| `leave_date` | date | Within request range; chargeable date |
| `slot` | enum | `AM` or `PM` |
| `active` | boolean | True for `PENDING` and `APPROVED`; false for terminal inactive occupancy |

Unique request slot: `(request_id, leave_date, slot)`. Partial unique active occupancy: `(employee_id, leave_date, slot) WHERE active`. A full day creates two rows; a half day creates one.

### LeaveRequestBalanceLine

Allocation of a tracked request across one or more balance periods.

| Field | Type | Rules |
|---|---|---|
| `id` | UUID | Primary key |
| `request_id` | UUID | FK to leave request |
| `balance_id` | UUID | FK to leave balance |
| `units` | integer | Positive |
| `state` | enum | `RESERVED`, `CONSUMED`, `RELEASED`, `RESTORED` |
| `updated_at` | timestamptz | Required |
| `version` | bigint | Exactly-once transition guard |

Unique key: `(request_id, balance_id)`. Sum of line units equals request chargeable units when the policy tracks balance. State transitions must match request status.

### LeaveBalanceMovement

Append-only ledger supporting reconstruction and audit of balance summaries.

| Field | Type | Rules |
|---|---|---|
| `id` | UUID | Primary key |
| `balance_id` | UUID | FK to balance |
| `request_id` | UUID nullable | Present for request-driven movements |
| `movement_type` | enum | `ALLOCATE`, `RESERVE`, `CONSUME_RESERVED`, `RELEASE_RESERVED`, `RESTORE_CONSUMED`, `ADMIN_ADJUST` |
| `units` | integer | Signed effect appropriate to movement type; nonzero |
| `reason` | varchar(1000) nullable | Mandatory for `ADMIN_ADJUST` |
| `actor_user_id` | UUID | Required |
| `created_at` | timestamptz | Required |
| `idempotency_key` | varchar(100) nullable | Unique per actor/command where supplied |

### LeaveRequestStatusHistory

| Field | Type | Rules |
|---|---|---|
| `id` | UUID | Primary key |
| `request_id` | UUID | FK to request |
| `from_status` | enum nullable | Null only for initial submission |
| `to_status` | enum | Required |
| `actor_user_id` | UUID | Required |
| `comment` | varchar(1000) nullable | Decision/cancellation/correction explanation |
| `created_at` | timestamptz | Required |

### AuditEvent

Generic immutable record for all actions required by FR-027 and FR-028.

| Field | Type | Rules |
|---|---|---|
| `id` | UUID | Primary key |
| `actor_user_id` | UUID | Required |
| `action` | varchar(80) | Stable code such as `LEAVE_SUBMITTED` |
| `entity_type` | varchar(80) | Stable aggregate type |
| `entity_id` | UUID | Referenced aggregate identifier |
| `occurred_at` | timestamptz | Required |
| `reason` | varchar(1000) nullable | Mandatory for adjustments/corrections |
| `before_data`, `after_data` | jsonb nullable | Minimal non-secret changed values |
| `request_correlation_id` | varchar(100) nullable | Diagnostic correlation, not authentication data |

Database permissions and repository interfaces expose inserts and reads only for status history, balance movements, and audit events.

## State transitions and balance effects

| Command | Allowed current status | Result | Balance-line effect | Active slots | Authority |
|---|---|---|---|---|---|
| Submit | None | `PENDING` | Create `RESERVED` lines and increment reserved units when validation is required | Insert active | Employee owner |
| Approve | `PENDING` | `APPROVED` | `RESERVED` to `CONSUMED`; decrement reserved and increment consumed | Remain active | Direct manager only; never self |
| Reject | `PENDING` | `REJECTED` | `RESERVED` to `RELEASED`; decrement reserved | Deactivate | Direct manager only; never self |
| Cancel pending | `PENDING` | `CANCELLED` | `RESERVED` to `RELEASED`; decrement reserved | Deactivate | Employee owner |
| Cancel approved | `APPROVED` and before cutoff | `CANCELLED` | `CONSUMED` to `RESTORED`; decrement consumed | Deactivate | Employee owner |
| Exceptional correction | Any, with explicit valid correction | Corrected state | Exact compensating movement; never rewrite ledger | Match corrected state | Administrator only, reason required |

No ordinary command exits `REJECTED` or `CANCELLED`. Every transition inserts exactly one status-history record and at least one matching audit event in the same transaction.

## Calculation and validation rules

1. Resolve the effective policy for each requested date and organization time zone.
2. Reject an inverted range, inactive employee/type, invalid policy period, or a half-day on a disallowed type.
3. For half-day mode, require one date and create the selected `AM` or `PM` slot.
4. For full-day mode, traverse the inclusive range. Apply configured weekly-off and holiday treatment, creating `AM` and `PM` slots for each chargeable date.
5. Reject a result with zero chargeable units.
6. Check active overlap before insert; rely on the active-slot unique index for concurrent races.
7. Allocate tracked units to applicable balance periods. For validated types, lock balances in `(period_start, id)` order and require sufficient unreserved units before reserving.
8. Return the same authoritative calculation DTO from preview and submission; submission recalculates rather than trusting client totals.

Approval repeats eligibility, policy, duration, overlap, and balance-line validation. A material policy/calculation change returns a conflict requiring the employee to submit a new request rather than silently changing approved duration.

## Required indexes and constraints

- Unique indexes on normalized login, employee number, leave type code, holiday date, policy version number, and applicable idempotency keys.
- Check constraints for date order, positive chargeable/movement units, nonnegative reserved/consumed/allocated units, cutoff days, and half-day single-date shape.
- Exclusion or equivalent constraints preventing overlapping policy versions and balance periods for the same owner/type.
- Partial unique active-slot index enforcing employee/date/AM-or-PM overlap protection.
- Request indexes on `(employee_id, submitted_at desc)`, `(employee_id, status, start_date)`, and `(status, start_date)`.
- Employee index on `manager_id` for reporting-scope queries.
- Balance index on `(employee_id, leave_type_id, period_start, period_end)`.
- Holiday index on active date; history/audit indexes on aggregate reference and descending timestamp.
- Report queries use the request status/type/date indexes and remain paginated; add specialized indexes only from measured query plans.

## Migration outline

1. `V001__identity_and_people.sql`: organization settings, accounts, roles, account roles, employees, reporting constraints and indexes.
2. `V002__leave_policy_and_calendar.sql`: leave types, effective policy versions, weekly offs, holidays and nonoverlap constraints.
3. `V003__leave_balances.sql`: balance periods, balance summaries, movements and integrity constraints.
4. `V004__leave_requests.sql`: requests, occupied slots, balance allocation lines, active-overlap constraint and query indexes.
5. `V005__history_and_audit.sql`: request status history, audit events, append-only permissions/rules and indexes.
6. Later changes use new forward migrations. Local demo reference data is isolated under a development-only Flyway location and is never enabled in production.
