import { NavLink, Outlet } from 'react-router-dom'
import { useAuth } from '../providers/AuthProvider'
export function AppShell(){const auth=useAuth();const manager=auth.principal?.roles.includes('MANAGER');const admin=auth.principal?.roles.includes('ADMINISTRATOR');return <><header><strong>Leave Management</strong><nav aria-label="Leave navigation">{auth.principal?.roles.includes('EMPLOYEE')&&<><NavLink to="/employee">Dashboard</NavLink><NavLink to="/employee/request">Request leave</NavLink><NavLink to="/employee/requests">History</NavLink><NavLink to="/employee/balances">Balances</NavLink><NavLink to="/employee/holidays">Holidays</NavLink><NavLink to="/employee/calendar">Team calendar</NavLink></>}{manager&&<><NavLink to="/manager">Manager dashboard</NavLink><NavLink to="/manager/requests">Approvals</NavLink><NavLink to="/manager/calendar">Manager calendar</NavLink></>}{admin&&<><NavLink to="/admin">Administrator</NavLink><NavLink to="/admin/reports">Reports</NavLink><NavLink to="/admin/audit">Audit history</NavLink></>}</nav><span>{auth.principal?.displayName}</span><button onClick={()=>void auth.logout()}>Sign out</button></header><main><Outlet/></main></>}
